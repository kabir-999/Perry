fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1

sqlalchemy[asyncio]==2.0.36
asyncpg>=0.29.0
alembic==1.14.0

httpx==0.28.1
beautifulsoup4==4.12.3
dnspython==2.7.0

PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.2.0

python-dotenv==1.0.1

gitpython==3.1.43

# Real-browser crawling for JS-heavy SPAs (Angular/React/Vue). One-time
# setup after `pip install -r requirements.txt`:
#     playwright install chromium
# The browser crawler degrades gracefully (falls back to the static
# crawler) if Chromium isn't installed — this is not a hard runtime dep.
playwright==1.62.0

# JS/TS AST parsing for sast_engine.py's taint tracker (pinned — was
# previously an undeclared transitive dependency).
esprima==4.0.1

# Polyglot AST parsing for the SAST taint tracker (Java/Go/PHP/Ruby/Rust/C#).
# Prebuilt wheels, no compiler toolchain needed. Degrades gracefully — SAST
# for these 6 languages is skipped (never silently, see the CLI's
# `languages` coverage report) if these aren't installed; Python/JS/secret
# scanning are unaffected.
tree-sitter==0.26.0
tree-sitter-language-pack==1.14.3

pytest==8.3.4
pytest-asyncio==0.25.2
