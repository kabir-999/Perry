fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1

sqlalchemy[asyncio]==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

httpx==0.28.1
beautifulsoup4==4.12.3
dnspython==2.7.0

PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.2.0

groq==0.13.1

python-dotenv==1.0.1

gitpython==3.1.43

# Real-browser crawling for JS-heavy SPAs (Angular/React/Vue). One-time
# setup after `pip install -r requirements.txt`:
#     playwright install chromium
# The browser crawler degrades gracefully (falls back to the static
# crawler) if Chromium isn't installed — this is not a hard runtime dep.
playwright==1.62.0

pytest==8.3.4
pytest-asyncio==0.25.2
