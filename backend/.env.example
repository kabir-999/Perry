# Copy the root-level .env.example instead: ../.env.example
# This file exists only to mirror the project structure spec; the app
# reads .env from the project root by default (see app/config.py).

GROQ_API_KEY=
GROQ_MODEL=
DATABASE_URL=
